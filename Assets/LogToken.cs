using UnityEngine;
using System.Threading.Tasks;
public class LogToken : MonoBehaviour
{/*
   [SerializeField] private HyplaySettings settings; // assign the hyplay settings in the editor
   async void Start ()
   {
        while (!HyplayBridge.IsLoggedIn) await Task.Yield();
        Debug.Log(settings.Token);
   }*/
}